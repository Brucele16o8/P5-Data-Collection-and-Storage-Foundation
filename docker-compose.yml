services:
  mongodb:
    image: mongo:7
    container_name: mongo-explore
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - mongodb_data:/data/db
      - ./data:/data/import
    restart: unless-stopped

volumes:
  mongodb_data:
